version: '3.11'

services:
  web:
    platform: linux/amd64
    image: rafacom/project_with_posts
    build: .
    ports:
      - "80:80"
    volumes:
      - ./uploads/images:/app/uploads/images
      - ./posts/posts.json:/app/posts/posts.json
    restart: always

